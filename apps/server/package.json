{
  "name": "@emberveil/server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'no lint configured for server'"
  },
  "dependencies": {
    "@emberveil/shared": "workspace:*",
    "fastify": "^5.1.0",
    "socket.io": "^4.8.1",
    "zod": "^3.24.1"
  }
}
